# Safe Calc Web

Desktop/web dashboard for TravelSafetySOS.

## Run locally

```bash
npm install
npm run dev
```

Set `VITE_BACKEND_URL` in `.env` to the deployed API or `http://localhost:5000` for local backend testing.
